
🚀 Day 10 – PayPal SQL Interview (Fun + Smart)

📌 Business Problem

PayPal wants to flag **suspicious users**.

A user is considered suspicious if:

* They made **3 or more transactions in a single day**
* AND the **total transaction amount that day > 10,000**

🎯 Interview Task
Find suspicious users and the date they were suspicious.

Output:
* `user_id`
* `txn_date`
* `txn_count`
* `total_amount`

🧠 Why Interviewers Like This Question

* No fancy tricks
* No overengineering
* Just **correct grouping + clear conditions**

Many candidates:
❌ Jump to window functions
❌ Overthink the logic

Strong candidates:
✅ Read the problem carefully
✅ Aggregate at the right level
✅ Use `HAVING` correctly


📌 Interview Insight:
This exact pattern is used in:

* Fraud detection
* Risk monitoring
* Compliance dashboards

